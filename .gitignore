# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
**/__pycache__/
*.pyc
*.pyo
.pytest_cache/

# ── OS / 에디터 ───────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.claude/

# ── Nuitka 빌드 결과물 ────────────────────────────────────────────────────────
# dist/ 전체 제외 (DLL, exe 등 수백 MB)
dist/
*.dist/
app.build/

# ── 데이터 / 바이너리 assets (git에 올리지 않음) ─────────────────────────────
# 로컬 셋업 방법: assets/ 폴더에 아래 항목을 직접 배치
#   - exiftool.exe  (https://exiftool.org)
#   - my_cities.csv  (루트의 my_cities.csv 복사)
#   - Natural Earth_10m_admin_0_countries/  (루트 폴더 복사)
assets/

# ── 지리 데이터 원본 (루트) ───────────────────────────────────────────────────
Natural Earth_10m_admin_0_countries/
cities1000.txt

# ── ExifTool 바이너리 (루트 또는 하위) ───────────────────────────────────────
exiftool.exe
exiftool_files/
exiftool-*/

# ── 파이프라인 중간 산출물 CSV ────────────────────────────────────────────────
pipeline_*.csv
pipeline_*.db
*_latest.csv
*_latest.db
reclassified_*.csv
reclassified_*.db
reindex_*.csv
root_recursive_*.csv
move_plan*.csv
classified_output/
PhotoClassifier_Release/

# ── 테스트 / 출력 데이터 ─────────────────────────────────────────────────────
other/
test_images/
*test*.csv
*test*.db

# ── SQLite DB ────────────────────────────────────────────────────────────────
*.db

# ── 임시 / 로그 ──────────────────────────────────────────────────────────────
*.log
*.tmp
*.bak
*.docx
*.pyd
*.dll

# ── 미디어 원본 파일 ─────────────────────────────────────────────────────────
*.jpg
*.jpeg
*.png
*.heic
*.heif
*.dng
*.tif
*.tiff
*.webp
*.mov
*.mp4
*.m4v
*.avi
*.mkv
*.3gp
*.arw
*.raw
*.cr2
*.nef
*.orf
*.rw2
